const name = "hitesh"
const repoCount = 50

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
// javascripts is using backsticks `` and ${} because this is called a template 
// literal or string interpplation
//  SIMPLE WORDS --- if u want to insert variables naturally inside a string
//                    use `text ${variable} text`
//   so backsticks create the template literal abd ${} inserts a value into it.
//  A TEMPLATE LITERAL 
// const name = "nisha"
// console.log(`hello ${name});

//  const gameName = new String('hiteshhc')

//  console.log(gameName[0]);
//  console.log(gameName.toUpperCase());
//  console.log(gameName.charAt('2'));
//  console.log(gameName.length);
//  console.log(gameName.indexOf('t'));

const newString = gameName.substring(0, 4)
console.log(newString);
const anotherString = gameName.slice(-8, 4)
console.log(anotherString);

// .trim(); read the usage
//  .replace(); read this also what it works

// .spilt(); read the usage of this function also 
// also read the different functions og strings
